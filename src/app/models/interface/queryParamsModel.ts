export type QueryParamsModel = {
  PageSize?: number;
  pageSize?: number;
  PageNumber?: number;
  pageLength?:number;
  pageNumber?:number;
  // searchWord?: string,
  search?: string,
  // StartDate?: string;
  // EndDate?: string;
  // Recent?: boolean;
  // Oldest?: boolean;
  // AlphabeticalOrder?: boolean;
  // Status?: string;
  // Region?: string;
};
