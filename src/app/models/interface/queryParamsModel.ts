export type QueryParamsModel = {
  PageSize?: number;
  PageNumber?: number;
  pageLength?:number;
  pageNumber?:number;
  searchWord?: string,
  // StartDate?: string;
  // EndDate?: string;
  // Recent?: boolean;
  // Oldest?: boolean;
  // AlphabeticalOrder?: boolean;
  // Status?: string;
  // Region?: string;
};
